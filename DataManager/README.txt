Logic that provides independent data processing on server-side(project - TPred)

config.py - is used to configure connection string to database and neural network parameters
warmup.py - script which is used to prepare database and all datamanager inner logic for work
services.py - here we have functions that main must execute, '_' is required before all executed function